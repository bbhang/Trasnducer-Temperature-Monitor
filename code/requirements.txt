# ICE Transducer Temperature Monitor dependencies
# (tkinter ships with the standard CPython installer on Windows)
pyvisa>=1.13
matplotlib>=3.7
numpy>=1.24
# A VISA backend is required for instrument access, either:
#   - NI-VISA / Keysight IO Libraries (recommended, install separately), or
#   - the pure-Python backend:  pip install pyvisa-py pyusb
